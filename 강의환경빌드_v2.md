# 강의환경 실행 안내 v2

이 문서는 수강생용 저장소를 받은 뒤 Docker 실습 환경을 실행하는 최소 안내서다.

## 준비물

- macOS Apple Silicon: OrbStack 또는 Docker Desktop
- Windows x64: Docker Desktop
- Docker Compose v2와 Docker 저장소 20GB 이상 여유 공간

## 실행

```sh
cp .env.example .env

# macOS Apple Silicon
docker compose --env-file env/macos-arm64.env up --build -d

# Windows x64
docker compose --env-file env/windows-amd64.env up --build -d
```

`.env`의 비밀번호는 개발용 예시다. 강의 전에 대문자·소문자·숫자를 포함하는 별도 값으로 바꾼다.

## 확인

```sh
./scripts/verify-compose.sh env/macos-arm64.env
curl http://localhost:8080/api/health
```

정상 응답은 `{"status":"ok","database":"connected"}`이다. 화면은 `http://localhost:5173`에서 연다.

## Codex CLI

Codex CLI는 앱 컨테이너에 설치되어 있다. 호스트에서 Codex에 로그인했다면
`${HOME}/.codex/auth.json`이 컨테이너의 `/tmp/codex-home/auth.json`에 읽기
마운트되어 별도 로그인 없이 사용할 수 있다. `config.toml`도 같은
`CODEX_HOME`에서 자동으로 읽는다.

```sh
./scripts/codex.sh login
./scripts/codex.sh
```

인증 마운트 또는 `infra/app/entrypoint.sh` 변경 후에는 앱 이미지를 다시
빌드해 컨테이너를 재생성한다. 인증 파일을 Dockerfile에 `COPY`하지 않으며,
Git에도 추가하지 않는다.

```sh
docker compose --env-file env/macos-arm64.env up -d --build app
```

수강생 배포물에는 교육자 전용 스킬과 제3자 스킬 파일을 포함하지 않는다.

## 문제 해결

- 저장 공간 부족: `docker system df` 결과를 확인하고 소유자 승인 없이 이미지를 삭제하지 않는다.
- OrbStack BuildKit 오류: `DOCKER_BUILDKIT=0 docker compose --env-file env/macos-arm64.env up --build -d`를 한 번만 사용한다.
- DB ARM 이미지 오류: `env/macos-amd64-fallback.env`로 재시도한다.
- API가 503: `docker compose logs --tail=120 db app`으로 DB 사용자 생성과 접속 오류를 확인한다.

중지는 다음 명령으로 한다. 데이터는 named volume에 유지된다.

```sh
docker compose down
```
