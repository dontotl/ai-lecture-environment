# 강의환경 초기 구성 v1

## 목표

macOS OrbStack와 Windows Docker Desktop에서 동일한 Docker Compose 구성으로 Oracle AI Database 26ai Free 및 FastAPI/Vite/Codex CLI 개발 환경을 실행한다.

## 구성

- `db`: `container-registry.oracle.com/database/free:latest-lite`를 확장한 Oracle AI Database 26ai Free Lite 컨테이너
- `app`: `container-registry.oracle.com/os/oraclelinux:8` 기반 FastAPI, Vite, Codex CLI 컨테이너
- 두 서비스는 Compose 기본 네트워크에서 통신하며, DB는 호스트 포트를 노출하지 않는다.

## 플랫폼과 리소스

- macOS Apple Silicon: `linux/arm64`
- Windows x64: `linux/amd64`
- 기본 런타임 메모리 목표: 4GB
  - `db.mem_limit: 2500m`
  - `app.mem_limit: 1250m`
- DB 초기화 실패 또는 Codex·IDE 동시 사용 시 런타임 메모리를 6GB로 올린다.
- Oracle Free Lite 이미지와 데이터 볼륨을 위해 OrbStack/Docker 저장소에 최소 20GB의 여유 공간을 확보한다. 기존 중지 컨테이너와 이미지는 삭제 전 소유자 확인을 거친다.
- Oracle 데이터는 `oracle-data` named volume에 보존하고, Vite 의존성은 `frontend-node-modules` named volume에 보존한다.

## 이미지 정책

- `app`은 Oracle Linux 8 전체 이미지를 사용하므로 `yum`과 `dnf`를 사용한다.
- `db`는 Free Lite 이미지 기반 확장 이미지다. 내장 `dnf`를 사용하고, 별도 `yum` 패키지를 내려받지 못하는 Lite 이미지에서는 DNF 호환 `yum` 명령을 심볼릭 링크로 제공한다.
- `db` 패키지 점검은 `docker compose exec -u root db dnf ...` 또는 `yum ...`으로 수행한다.
- 앱·DB 이미지 모두 OS별 `CONTAINER_PLATFORM`으로 빌드·실행한다. Apple Silicon에서 Oracle DB ARM64 매니페스트가 없으면 `linux/amd64`로 폴백한다.
- 이 네트워크에서는 npm 공식 Registry가 보안 게이트웨이에서 차단되므로, 이미지 빌드와 Vite 의존성 설치에는 TLS 검증이 가능한 `https://registry.npmmirror.com`을 명시적으로 사용한다. npm 설정을 사용자 홈에 영구 변경하지 않고 프로젝트 로컬 캐시 또는 컨테이너 임시 캐시만 사용한다.

## 앱 동작

- FastAPI `GET /api/health`는 Oracle `FREEPDB1`에 `SELECT 1 FROM dual`을 실행한다.
- 정상: `200 {"status":"ok","database":"connected"}`
- DB 오류: `503 {"status":"degraded","database":"unavailable"}`
- Vite 화면은 API 상태를 연결됨, DB 오류, 네트워크 오류로 구분해 표시한다.
- Vite `/api` 프록시는 같은 컨테이너의 `http://127.0.0.1:8000`을 대상으로 한다.

## Codex 프로필

`.codex/skills`의 27개 선택 스킬은 `scripts/sync-codex-skills.sh`로 실제 파일 형태로 `infra/codex-profile/skills`에 복사한다. Dockerfile은 이 프로필을 `/opt/codex-profile`에 넣고, entrypoint는 컨테이너 시작마다 `/tmp/codex-home`으로 복사한다. 인증 정보는 이미지·저장소·영속 볼륨에 저장하지 않는다.

포함 스킬:

- `archify`, `docx`, `frontend-design`, `webapp-testing`, `pdf`, `pptx`, `xlsx`
- `frontend-app-builder`, `frontend-testing-debugging`, `react-best-practices`, `shadcn-best-practices`, `stripe-best-practices`, `supabase-best-practices`
- `brainstorming`, `dispatching-parallel-agents`, `executing-plans`, `finishing-a-development-branch`, `receiving-code-review`, `requesting-code-review`, `subagent-driven-development`, `systematic-debugging`, `test-driven-development`, `using-git-worktrees`, `using-superpowers`, `verification-before-completion`, `writing-plans`, `writing-skills`

## 실행

`.env`의 Oracle 계정 비밀번호는 대문자·소문자·숫자를 포함해야 한다. 제공된 예제 값은 개발용이므로,
강의 시작 전에는 각각 별도의 안전한 값으로 변경한다.

```sh
# macOS Apple Silicon / OrbStack
docker compose --env-file env/macos-arm64.env up --build

# Windows x64 / Docker Desktop
docker compose --env-file env/windows-amd64.env up --build
```

현재 OrbStack 환경에서 BuildKit 세션 헤더 오류가 발생하면, 한 번의 로컬 빌드에만 아래 우회를 사용한다. Docker Desktop/OrbStack 업데이트 후에는 기본 BuildKit 빌드를 다시 우선한다.

```sh
DOCKER_BUILDKIT=0 docker compose --env-file env/macos-arm64.env up --build
```

Codex는 앱 컨테이너에서 실행한다.

```sh
docker compose exec -it app codex login
docker compose exec -it app codex
```

## 검증 기준

- macOS와 Windows에서 `db`, `app` 두 컨테이너가 실행된다.
- 두 컨테이너에서 root 기준 `yum`과 `dnf` 명령이 실행된다.
- DB는 외부 포트 없이 app에서만 `db:1521/FREEPDB1`로 접속된다.
- Free Lite의 사전 구성 DB는 `/opt/oracle/scripts/setup`을 건너뛸 수 있으므로, 멱등적인 앱 사용자 생성 스크립트를 `/opt/oracle/scripts/startup`에 마운트해 매 기동 후 실행한다.
- `/api/health`, Vite 상태 화면, Codex CLI, 27개 선택 스킬을 검증한다.
