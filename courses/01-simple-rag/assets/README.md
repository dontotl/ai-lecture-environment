# 선택적 시각 자산

이 디렉터리는 이후 강의에 필요한 로고·스크린샷·이미지를 두는 자리다. 현재 시나리오에는 추가 이미지가 필요 없다.

자산을 추가하면 출처와 배포 허용 조건을 함께 기록한다. 개인 화면, API 키, 실제 회사 내부 자료는 포함하지 않는다. 생성된 Archify 파일은 구현 저장소의 `static/architecture/`에 두며 이 자료 디렉터리에 덮어쓰지 않는다.
